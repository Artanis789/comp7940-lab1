name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Deploy
        env:
          HEROKU_API_KEY: ${{secrets.HEROKU_API_KEY}}
          HEROKU_APP_NAME: "comp7940artanis"
        run: git push -f
https://heroku:$5bde8810-326f-4d0d-8411-ad2807085256@git.heroku.com/$comp7940artanis.git main        